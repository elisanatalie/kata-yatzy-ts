export function ones(d1: number, d2: number, d3: number, d4: number, d5: number): number {
  var sum = 0;
  if (d1 == 1) sum++;
  if (d2 == 1) sum++;
  if (d3 == 1) sum++;
  if (d4 == 1) sum++;
  if (d5 == 1) sum++;

  return sum;
}
